// 18. find() method
/* returns the first element that satisfies a condition.

return : the element itself, or undefined if not found

*/

let num = [2, 4, 6, 8];

let check1 = nums.find((x) => x % 2 == 0);

// function test(x) {
//     return x > 0;
// }
console.log(check1);
